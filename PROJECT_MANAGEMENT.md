# 项目管理：通用招聘官网简历填写

更新时间：2026-08-30

## 1. 项目目标

让用户通过 Codex、WorkBuddy 等 Agent，在中国企业招聘官网完成“读取页面 -> 发现字段 ->
映射本地简历 JSON -> 用户确认 -> 填写 -> 复核”的安全流程。站点适配器只能作为经过验证
的结构缓存，不能成为支持新网站的前置条件。

成功标准是“高覆盖率 + 可解释的人工接管”，而不是对任意网页做无提示的盲填。

## 2. 当前版本

- 版本标签：`0.2-generic-discovery`
- 通用能力基线提交：`d66d29a7e301f6d3a3fee059db090cab436803b`（后续提交以 GitHub `main` 为准）
- 当前运行目录：`D:\Job\Resume`
- 当前项目目录：`D:\market\投递Skill\resume-form-filler-project`
- 公开仓库：[Sloan-Z/-skill](https://github.com/Sloan-Z/-skill)

## 3. 当前已经具备的能力

| 能力 | 当前行为 | 状态 |
| --- | --- | --- |
| 简历标准化 | 从明确提供的 PDF/DOCX/图片/文本生成 `draft` JSON，校验后再确认 | 可用 |
| 通用页面发现 | 读取可见 input、select、textarea、ARIA 控件、contenteditable | 可用 |
| 语义分区 | 根据标题、legend、字段标签识别基本信息、教育、工作/实习、项目、校园、获奖、技能等 | 可用，需置信度确认 |
| 字段定位 | 生成当前 DOM 内的 CSS locator，按分区和记录序号定位 | 可用 |
| 简历映射 | 中文字段别名映射到基本信息、教育、实习、项目等 JSON 路径 | 可用，词典仍需扩充 |
| 描述复用 | 实习 `achievements` 合并；项目 `description + achievements` 合并 | 可用 |
| 动态记录 | 预览不点击添加；确认后识别明确的添加入口，添加后重新读取 | 部分可用 |
| 低置信度处理 | 对不确定、单选、下拉、复选标记 `needs-confirmation` | 可用 |
| 缺失字段回访 | 生成问题，回答写入新的 `draft`，不覆盖原 JSON | 可用 |
| 适配器降级 | 无适配器或指纹失效时进入 generic discovery | 可用 |
| 安全边界 | 不提交、不保存、不上传、不填密码/验证码/MFA，默认不覆盖已有内容 | 可用 |

## 4. 当前做不到或不可靠的能力

这些事项不是“隐藏功能”，而是当前版本必须明确交还用户的边界。

| 编号 | 能力缺口 | 当前原因 | 现行处理 |
| --- | --- | --- | --- |
| L1 | 跨域 iframe 内的表单自动发现和填写 | 每个 frame 有独立 DOM 和权限边界，当前 reader 只读主文档 | 标记为未发现，用户手动处理 |
| L2 | shadow DOM，尤其 closed shadow root | 当前 CSS locator 不穿透 closed root，也没有组件宿主协议 | 用户手动处理；不猜坐标 |
| L3 | 被折叠、分页、虚拟列表或懒加载的字段 | 预览阶段不主动点击页面状态控件，DOM 中可能没有字段 | 报告当前可见范围，用户展开后重新读取 |
| L4 | 无语义标签的自绘控件 | 没有 label/ARIA/name 时无法可靠知道字段含义 | `manual` 或 `needs-confirmation` |
| L5 | 复杂级联下拉、搜索联想、日期选择器 | 选项依赖异步请求和前置选择，当前只稳定处理简单文本/原生选择 | 保留人工填写 |
| L6 | 任意语言、行业术语和企业自定义字段的高精度语义映射 | 当前主要是中文别名词典，没有本地领域本体和系统化语义模型 | 低置信度确认，不自动猜 |
| L7 | 从截图/视觉布局恢复完整字段语义 | 当前 generic reader 以 DOM/可访问性为主，没有视觉 OCR fallback | 仅 DOM 能见字段进入映射 |
| L8 | 任意动态“添加/删除/排序”操作 | 当前只识别明确文本添加按钮，不理解复杂状态机和删除副作用 | 只在安全入口明确时扩展，否则人工 |
| L9 | 页面级业务校验的自动修复 | 可以复核控件值，但不了解每个网站的业务规则、联动错误和服务端校验 | 停止并提示用户处理 |
| L10 | 自动登录、验证码、滑块、短信、MFA、反自动化风控 | 安全和权限边界禁止绕过 | 永远交还用户 |
| L11 | 自动保存、提交、投递和附件上传 | 会产生不可逆外部副作用，且内容责任必须由用户承担 | 永远由用户手动完成 |
| L12 | 证明“任意网站都能成功”的量化保证 | 目前缺少大规模匿名 fixture 基准和真实网站回归数据 | 只能给出单页结果与置信度 |

## 5. 目标能力分级

| 级别 | 目标 | 验收口径 |
| --- | --- | --- |
| G0 | 当前通用 DOM 路径 | 无适配器陌生页面可读取、映射、预览，失败可解释 |
| G1 | 多文档、多状态发现 | 同源 frame、open shadow、折叠分区、滚动懒加载覆盖率显著提升 |
| G2 | 控件策略 | 原生/自定义选择、联想、日期范围、富文本有可验证策略 |
| G3 | 语义泛化 | 中英文和企业自定义标签有本地本体 + 模型辅助，低置信度不盲填 |
| G4 | 视觉和人工协同 | DOM 缺失时提供截图/OCR 候选，用户逐字段确认后才操作 |
| G5 | 可持续工程化 | 匿名 fixture 基准、回归门禁、可观测性、插件 API 和版本兼容策略 |

## 6. 风险与不变原则

- **误填风险最高**：宁可 `manual`，不得把相似字段强行映射。
- **隐私风险**：默认不输出值；只处理用户明确提供的简历；不把个人数据放进仓库或测试 fixture。
- **外部副作用**：提交、保存、投递、上传、登录和验证永远不自动执行。
- **站点变化**：适配器指纹失效必须回退通用发现，不能继续使用旧 locator。
- **反自动化**：不降低等待、验证或人工确认门槛来追求成功率。

## 7. 项目节奏

1. P0：补齐通用发现的多 frame、open shadow、可见状态和安全 locator。
2. P1：建立字段本体、别名版本和置信度校准，完善动态分区协议。
3. P1：实现可验证的原生/自定义控件策略，统一失败与人工接管状态。
4. P2：加入视觉/OCR 只读候选和模型辅助映射，严格保留逐字段确认。
5. P2：建立匿名页面 fixture、覆盖率指标、回归测试和发布门禁。
6. P3：抽象 WorkBuddy/浏览器扩展插件 API，支持能力协商而不是复制站点适配器。

详细任务、依赖和验收条件见 [docs/ROADMAP.md](docs/ROADMAP.md)。
